# MatchingWithRegEx

Данный файл содержит необходимую информацию о подкаталогах проекта и о программе **MatchingWithRegEx**.

---

## Информация о подкаталогах проекта

### Корень репозитория

- `MatchingWithRegEx.sln` — решение Microsoft Visual Studio (основная программа и модульные тесты).
- `README.md` — описание проекта (этот файл).
- `.gitignore` — исключение артефактов сборки и служебных файлов IDE из системы контроля версий.

### Подкаталог `Documents/` (документация по разработке)

Связанные с разработкой программы документы:

- внешняя спецификация (техническое задание);
- внутренняя спецификация (структуры данных, алгоритмы функций);
- программа и методика испытаний (таблицы тестов).


### Подкаталог `MatchingWithRegEx/` (исходный код программы)

Проект на **C++** (стандартная библиотека). Основные модули:

| Файлы | Назначение |
|--------|------------|
| `main.cpp` | Точка входа, разбор аргументов командной строки, вызов цепочки обработки |
| `RegExTree.cpp`, `RegExTree.h` | Построение синтаксического дерева из ОПЗ, генерация фрагментов НКА |
| `NFA.cpp`, `NFA.h` | Структуры состояний и переходов НКА |
| `MatchLogic.cpp`, `MatchLogic.h` | Поиск совпадений, ε-замыкание, приоритет полного/частичного совпадения |
| `HtmlReport.cpp`, `HtmlReport.h` | Формирование HTML-отчёта |
| `Error.cpp`, `Error.h` | Коды и сообщения об ошибках |
| `Token.cpp`, `Token.h` | Разбор токенов ОПЗ |

### Подкаталог `MatchingWithRegEx/x64/Debug/` 

После сборки в Visual Studio:

- `MatchingWithRegEx.exe` — исполняемый файл программы 

### Подкаталог `MatchingWithRegExTests/` (модульные тесты)

Автоматические тесты на **Microsoft CppUnitTest Framework** (запуск из Visual Studio: **Тест** → **Обозреватель тестов**).

| Компонент | Назначение |
|-----------|------------|
| `RegExTreeTests.cpp` | Таблица 1 — построение AST из ОПЗ |
| `NFAGenerationTests.cpp` | Таблицы 2.1–2.2 — построение НКА и ε-замыкание |
| `MatchLogicTests.cpp` | Таблицы 3.1–3.4 — расстояния, симуляция, выбор совпадения, поиск в позиции |
| `IntegrationTests.cpp` | Интеграционный поиск по строке (`extractAllMatches`) |
| `TestHelpers.cpp`, `TestHelpers.h` | Сравнение деревьев, НКА, двусторонние проверки множеств |
| `ClosureGraphSetup.cpp` | Вспомогательные графы для тестов ε-замыкания |

## Информация о программе MatchingWithRegEx

### Назначение

Программа предназначена для **нахождения совпадений** анализируемой строки с **регулярным выражением в обратной польской записи (ОПЗ)**. Поддерживаются **полные** и **частичные** совпадения; для частичных учитывается кратчайший путь до допускающего состояния НКА.

Программа разработана на языке **C++** с использованием стандартной библиотеки C++.

### Аргументы командной строки (по внутренней спецификации)

Программа получает **три** аргумента:

1. путь к **входному файлу** с регулярным выражением (ОПЗ);
2. путь к **входному файлу** с проверяемой строкой;
3. путь к **выходному файлу** отчёта в формате **HTML** (`.html`).

При неверном числе аргументов или ошибках чтения файлов программа должна вывести понятное сообщение и завершить работу с ненулевым кодом возврата.

**Пример команды запуска (Windows):**

```text
MatchingWithRegEx.exe regex.txt string.txt result.html
```

или с относительными путями:

```text
.\MatchingWithRegEx\x64\Debug\MatchingWithRegEx.exe .\regex.txt .\string.txt .\result.html
```

### Входные данные

| Файл | Содержание |
|------|------------|
| **Первый** (шаблон) | Регулярное выражение в **постфиксной** форме. **Разделитель токенов — только пробел** (в т.ч. между операторами `&`, `|`, квантификаторами `{n,m}`, `*`, `+`, `?`). Допускаются экранированные символы (`\n`, `\t`, `\*` и т.д.). |
| **Второй** (строка) | Текст, в котором выполняется поиск всех непересекающихся совпадений. |

### Выходные данные (HTML)

**Третий** файл — **HTML-отчёт**:

- при **ошибках** в шаблоне — подсветка проблемных позиций в выражении;
- при успешном разборе — подсветка в строке:
  - **полные** совпадения — зелёная разметка (`<mark>`);
  - **лучшее частичное** совпадение — жёлтый фон (`<span>`);
  - несовпавшие фрагменты — красный текст.

Логика отбора совпадений реализуется функцией `extractAllMatches`; оформление — `generateHtmlReport` (см. `MatchLogic.h`, `HtmlReport`).

### Пример работы (ОПЗ и строка)

**Файл `regex.txt`** (одна строка, токены через пробел):

```text
o n e &3 t w o &3 t h r e e &5 |3 +
```

Смысл: конкатенации `one`, `two`, `three` и квантификатор «одно или более» над альтернативой из трёх веток.

**Файл `string.txt`:**

```text
onetwothree
```

**Ожидаемое поведение поиска** (несколько совпадений в одной строке, без пересечений; в отчёте — подсветка фрагментов):

| № | Позиция | Фрагмент |
|---|---------|----------|
| 0 | 0–3 | `one` |
| 1 | 0–6 | `onetwo` |
| 2 | 0–11 | `onetwothree` |
| 3 | 3–6 | `two` |
| 4 | 3–11 | `twothree` |
| 5 | 6–11 | `three` |

### Дополнительные примеры (из модульных тестов)

| ОПЗ | Строка | Смысл |
|-----|--------|--------|
| `a` | `a` | одно полное совпадение |
| `c a t &3` | `cat` | конкатенация «cat» |
| `c a t &3` | `ca` | частичное совпадение `{ca}` при обрыве строки |
| `a *` | `baaab` | жадный повтор символа `a` |
| `. *` | `xyz` | маска «любой символ», повтор 0 и более |

В интеграционных тестах результат кодируется так: **`[текст]`** — полное совпадение, **`{текст}`** — частичное, остальной текст — без совпадения.

### Сборка и тесты

1. Открыть `MatchingWithRegEx.sln` в **Visual Studio 2019/2022**.
2. Собрать конфигурацию **Debug** , платформа **x64**.
3. Запустить **MatchingWithRegExTests** через **Обозреватель тестов**.

---

**Автор:** Филиппова К.Е.  
**Дата:** 22 июня 2026  
**Версия:** 1.0
